pluginManagement {
  repositories {
    gradlePluginPortal()
    google()
  }
}

rootProject.name = 'eventbus-service'
include ':codegen', ':annotation', ':core'
if (!System.env.JITPACK)
  include ':example'
